easy_admin:
    entities:
        Filter:
            class: '%krg_cms.filter.class%'
            list:
                fields:
                    - enabled
                    - working
                    - key
                    - name
            form:
                fields:
                    - enabled
                    - name
                    - { property: key, type_options: { required: true } }
                    - { property: form, type: KRG\CmsBundle\Form\Type\FilterType }
